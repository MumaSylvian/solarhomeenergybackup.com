/* oxlint-disable next/no-html-link-for-pages -- policy navigation must work without the client router. */
import { ArrowRight } from 'lucide-react';
type Section = { title: string; body: string };
export function TrustPage({ eyebrow, title, intro, sections }: { eyebrow: string; title: string; intro: string; sections: Section[] }) {
  return <main className="page-shell trust-page"><header><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p>{intro}</p></header><div className="trust-layout"><section>{sections.map((section) => <article className="trust-section" key={section.title}><h2>{section.title}</h2><p>{section.body}</p></article>)}</section><aside className="aside-card"><h2>Shop with clarity</h2><p>Review delivery, warranty, returns, and payment information before you place an order.</p><a href="/shipping-delivery">Shipping & delivery <ArrowRight size={15}/></a><a href="/returns">Returns & refunds <ArrowRight size={15}/></a><a href="/warranty">Warranty <ArrowRight size={15}/></a><a href="/privacy">Privacy <ArrowRight size={15}/></a><a href="/terms">Terms <ArrowRight size={15}/></a><a href="/payment-options">Payment options <ArrowRight size={15}/></a><a href="/invoice">Request an invoice <ArrowRight size={15}/></a></aside></div></main>;
}
